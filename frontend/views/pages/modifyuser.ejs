<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Final</title>
</head>
<body class="container">
    <header>
        <!-- bootstrap link -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
        <%- include('../partials/header'); %>
    </header>
    <div class="jumbotron">
        <div class="card">
            <h2 class="display-5">Select user to edit:</h2>
            <div class="card-body">
                <!-- form to modify user in db, posts to modifyuser for api to read and send to back end -->
                <form name="useredit" id="useredit" action="/modifyuser" method="POST">
                    Friend ID:
                    <!-- dropdown menu containing all friends from DB -->
                    <select name="friendid" id="friendid">
                        <!-- uses for loop to populate drop down -->
                        <% for(var i=0; i < friends.length; i++) { %>
                            <option value=<%= friends[i].friendid %>><%= friends[i].friendid %> <%= friends[i].firstname %> <%= friends[i].lastname %></option>
                        <% } %>
                    </select>
                    <!-- this submit button has a different form action to allow user to delete selected user from db-->
                    <input type="submit" value="Delete" class="btn btn-danger" formaction="/deleteuser">
                    <br>
                    <br>
                    <!-- text boxes for user to specify new first and last name for friend id to be modified -->
                    New first name: 
                    <input type="text" name="firstname" id="firstname">
                    <br>
                    <br>
                    New last name: 
                    <input type="text" name="lastname" id="lastname">
                    <!-- this submit button has the form's original form action -->
                    <input type="submit" class="btn btn-success" value="Update">
                    
                </form>
                <br>
                <!-- button sends user back to add user page -->
                <a class="btn btn-primary" href="/adduser" role="button">Go Back to Add User</a>

            </div>  
        </div>
    </div>

    <footer>
        <!--footer templete from partials-->
        <%- include('../partials/footer'); %>
    </footer>
</body>
</html>